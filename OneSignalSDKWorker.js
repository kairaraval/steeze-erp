// OneSignal Web Push service worker. Imported by /sw.js (same-origin) so the
// browser can register push subscriptions correctly. Uploaded to the web root
// alongside sw.js, index.html, etc.
importScripts("https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.sw.js");
